// orla

//Class BarChart
//void setup(){
//  BarChart();
//}

class BarChart {
    // margin from chart bottom and gaps
    final float MARGINY = 350;
    final float GAP = 30;

    // inner class
    class CreateBarChart {
        // arrays w flight names and corresponding emissions
        ArrayList<String> flights;
        ArrayList<Integer> emissions;
        
        
        float barSize;          // bar width
        float largestFreq;      // highest emissions in dataset
        float multiple;         // a scaling variable
        float gap;              // gap between bars
        float barSpace;         // Space between each group of bars
        String xAxis;           
        String yAxis;           
        float MARGINX;          // margin from left
        
         //fill(255, 100, 200); // fill colour pink bars
         //rect(x, y, barSize, ?); // 
    
         //fill(0); // colour for text
         //textAlign(CENTER); //  text to center

    }
}
